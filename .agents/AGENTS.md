# Project Rules for Tamil Wordle

- **Git Push Policy**: Do NOT run `git push` automatically. Only commit locally when appropriate or requested, and wait for explicit approval from the user before executing `git push`.
